{
  "permissions": {
    "allow": [
      "Bash(cat:*)",
      "Bash(npm start)",
      "Bash(curl:*)",
      "Bash(powershell -Command \"Get-Process | Where-Object {$_ProcessName -like ''*vite*'' -or ($_CommandLine -like ''*vite*'')} | Select-Object Id, ProcessName, CommandLine | Format-Table\")"
    ]
  }
}
