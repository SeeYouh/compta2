{
  "name": "compta",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "yarn workspace client build",
    "start": "cd server && node src/server.js"
  }
}
