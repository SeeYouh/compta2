// Limites de la grille pour les pages du canvas document
export const DOCUMENT_GRID_COLUMNS_MIN = 2;
export const DOCUMENT_GRID_COLUMNS_MAX = 20;
export const DOCUMENT_GRID_ROWS_MIN = 2;
export const DOCUMENT_GRID_ROWS_MAX = 50;

// Format A4 en mm (référence pour l'export PDF)
export const A4_WIDTH_MM = 210;
export const A4_HEIGHT_MM = 297;

// Marges par défaut en mm
export const DOCUMENT_MARGINS_DEFAULT = {
  top: 10,
  bottom: 10,
  left: 10,
  right: 10,
};

// Grille par défaut d'une nouvelle page
export const DOCUMENT_PAGE_DEFAULT = {
  columns: 4,
  rows: 10,
};
